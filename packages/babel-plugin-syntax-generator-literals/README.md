# @babel/plugin-syntax-generator-literals

> Allow parsing of generator literals.

## Install

Using npm:

```sh
npm install --save-dev @babel/plugin-syntax-generator-literals
```

or using yarn:

```sh
yarn add @babel/plugin-syntax-generator-literals --dev
```
